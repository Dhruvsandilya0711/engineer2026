<%# Chapter joint. Purely decorative, so it is hidden from assistive tech —
    the heading of the next section is what actually announces the change. %>
<div class="seam" aria-hidden="true" data-reveal>
  <span class="seam__line"></span>
  <span class="seam__node"></span>
  <span class="seam__line"></span>
</div>
