node_modules
screenshots/

# Registration submissions contain personal data (names, roll numbers,
# emails) — the local file store must never be committed.
data/registrations.json

# Leaderboard runs carry player-entered names and hashed IPs — same reason.
data/leaderboard.json

# Secrets: DBURL and any Razorpay keys live here, never in the repo.
.env
.env.*
!.env.example

# Build/working artifacts
*.zip
screenshots$num-*.png
public/.DS_Store

# Static export for GitHub Pages (scripts/export-static.mjs)
dist/
